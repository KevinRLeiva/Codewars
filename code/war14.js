//given an array of integers, return the numbers of entries which are greater than 10
function countTo10(integers){
  let count = 0;
  for(let i = 0; i < integers.length; i++){
    count++
  }
  return count;
}
